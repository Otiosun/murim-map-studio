# Supabase

Database configuration, migrations and deterministic seed will begin in Foundation Phase 3.

Do not configure canonical schema only through the dashboard; migrations must be versioned in Git.
